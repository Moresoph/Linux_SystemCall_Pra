FIND_PACKAGE(HELLO)
IF(HELLO_FOUND)
	MESSAGE(STATUS "Found HELLO")
	ADD_EXECUTABLE(hello main.c)
	INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})
	TARGET_LINK_LIBRARIES(hello /usr/local/lib)
	#TARGET_LINK_LIBRARIES(hello ${HELLO_LIBRARY})
ELSE(HELLO_FOUND)
	MESSAGE("CAN'T FIND HELLO")
ENDIF(HELLO_FOUND)
