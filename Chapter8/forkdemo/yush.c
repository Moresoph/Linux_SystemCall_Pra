#include<stdio.h>
#include<stdlib.h>
#include<signal.h>

const int MAXARGS=20;
const int ARGLEN=100;
char* makestring(char *buf)
{
	char *cp;
	
}
void main()
{
	char *arglist[MAXARGS+1];
	char argbuf[ARGLEN];
	int num=0;
	while(num<MAXARGS)
	{
		printf("Arg[%d] is ?",num);
		if( fgets(
	}
}
